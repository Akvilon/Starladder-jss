import { Theme } from ".";

export const getRedColor = (theme: Theme) => theme.colors.red;
export const getBlackColor = (theme: Theme) => theme.colors.black;
export const getDarkGreyColor = (theme: Theme) => theme.colors.darkGrey;
export const getLightGreyColor = (theme: Theme) => theme.colors.lightGrey;
export const getWhiteColor = (theme: Theme) => theme.colors.white;
